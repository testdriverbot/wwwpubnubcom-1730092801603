version: 4.0.77
steps:
  - prompt: Click on "Pricing" in the header.
    commands:
      - command: hover-text
        text: Pricing
        description: link in the header
        action: click
  - prompt: >-
      Assert that the pricing page displays pricing plans clearly.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the pricing page displays pricing plans clearly
